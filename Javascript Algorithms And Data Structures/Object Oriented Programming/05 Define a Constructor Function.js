/**
* Task: Create a constructor, Dog, with properties name, color, and numLegs that
* are set to a string, a string, and a number, respectively.
*/
function Dog() {
  this.name = "dog";
  this.color = "blue";
  this.numLegs = 3;
}
